NEXT_PUBLIC_API_BASE_URL=http://localhost:4100/api

# Contributions & Fundraising (Phase 10). Public/publishable keys only — safe to expose
# in the browser bundle; the matching secret keys live in backend/.env.example instead.
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_replace_this
NEXT_PUBLIC_FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST-replace_this
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_this
